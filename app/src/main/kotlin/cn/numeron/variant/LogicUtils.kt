package cn.numeron.variant

class Util

fun calculate(a: Int, b: Int): String {
    return "main  calculate : " + (a + b*10)
}

object LogicUtilos {
    fun calculate(a: Int, b: Int): String {
        return "actuator calculate : " + (a + b)
    }
}